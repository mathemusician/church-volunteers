# Auth.js Configuration
AUTH_SECRET=# Generate with: openssl rand -base64 32

# ZITADEL OAuth Configuration
AUTH_ZITADEL_ISSUER=https://church-volunteers-vpchf7.us1.zitadel.cloud
AUTH_ZITADEL_ID=341538444725489255
AUTH_ZITADEL_SECRET=# Your ZITADEL client secret

# ZITADEL Organization ID (for org-scoped login and passkeys)
ZITADEL_ORG_ID=# Your organization ID from ZITADEL console

# ZITADEL Service User (for Management API calls like sending passkey links)
ZITADEL_SERVICE_CLIENT_ID=# Service user client ID
ZITADEL_SERVICE_CLIENT_SECRET=# Service user client secret
# OR use JWT for service authentication:
# ZITADEL_SERVICE_KEY_ID=# JWT key ID
# ZITADEL_SERVICE_USER_ID=# Service user ID
# ZITADEL_SERVICE_PRIVATE_KEY=# Base64-encoded private key

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Optional: Cron job secret for automatic event generation
CRON_SECRET=# Random secret for cron endpoint security
