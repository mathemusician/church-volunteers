export const metadata = {
  title: 'Account Settings - Volunteers',
  description: 'Manage your passkeys and security settings',
};

export default function AccountSettingsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
