export const metadata = {
  title: 'Volunteer Manager - Volunteers',
  description: 'Manage events, lists, and volunteer signups',
};

export default function VolunteerManagerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
