export const metadata = {
  title: 'Organization Settings - Volunteers',
  description: 'Update organization name and details',
};

export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
